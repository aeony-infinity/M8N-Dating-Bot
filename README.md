# M8N-Dating Bot

This is the initial version of the M8N-Dating Bot. This bot aims to assist users with dating-related interactions and activities.

## How to Run

1. Clone the repository.
2. Install dependencies using `pip install -r requirements.txt`.
3. Configure the bot by updating `config/settings.py`.
4. Run the bot with `python main.py`.
